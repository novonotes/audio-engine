name: audio_engine_examples
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  audio_engine:
    path: ../audio_engine
  audio_engine_flutter_libs:
    path: ../audio_engine_flutter_libs

  path: ^1.8.3
  shared_preferences: ^2.2.1
  fixnum: ^1.1.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  lints: ^2.1.1
  nam:
    path: ../nam
flutter:
  uses-material-design: true

  assets:
    - assets/example.wav
